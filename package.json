{
  "name": "figma-design-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Local REST and MCP bridge that imports Figma designs through the official API",
  "scripts": {
    "start": "node server/index.js",
    "mcp": "node server/mcp.mjs",
    "import:figma": "node server/import-figma.mjs",
    "test": "node --test",
    "inspect": "node server/index.js --inspect"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
